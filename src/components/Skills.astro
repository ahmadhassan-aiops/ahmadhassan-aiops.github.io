---
const skillDomains = [
  {
    id: 'ds-ml',
    label: 'Data Science & ML',
    icon: '🧠',
    color: 'purple',
    skills: [
      { name: 'Python', level: 5 },
      { name: 'NumPy', level: 5 },
      { name: 'Pandas', level: 5 },
      { name: 'Scikit-learn', level: 5 },
      { name: 'Matplotlib / Seaborn', level: 5 },
      { name: 'XGBoost / LightGBM', level: 4 },
      { name: 'Statistics & Probability', level: 5 },
      { name: 'Feature Engineering', level: 5 },
      { name: 'SQL', level: 4 },
      { name: 'Power BI', level: 4 },
      { name: 'Tableau', level: 4 },
      { name: 'Time Series Analysis', level: 4 },
    ],
  },
  {
    id: 'dl-ai',
    label: 'Deep Learning & AI',
    icon: '🤖',
    color: 'cyan',
    skills: [
      { name: 'TensorFlow / Keras', level: 5 },
      { name: 'PyTorch', level: 4 },
      { name: 'ANN / CNN', level: 5 },
      { name: 'RNN / LSTM', level: 4 },
      { name: 'Transformers', level: 4 },
      { name: 'Computer Vision (OpenCV)', level: 4 },
      { name: 'NLP / Text Processing', level: 4 },
      { name: 'Hugging Face', level: 4 },
      { name: 'LLM Fine-tuning', level: 4 },
      { name: 'Stable Diffusion', level: 3 },
      { name: 'LangChain', level: 4 },
      { name: 'Prompt Engineering', level: 5 },
    ],
  },
  {
    id: 'devops',
    label: 'DevOps & MLOps',
    icon: '⚙️',
    color: 'green',
    skills: [
      { name: 'Docker', level: 5 },
      { name: 'Kubernetes (K8s)', level: 4 },
      { name: 'Jenkins / CI-CD', level: 4 },
      { name: 'Ansible', level: 4 },
      { name: 'Terraform', level: 3 },
      { name: 'Git / GitHub', level: 5 },
      { name: 'ArgoCD', level: 3 },
      { name: 'Prometheus / Grafana', level: 4 },
      { name: 'AWS (EC2, S3, ECR, ECS)', level: 4 },
      { name: 'Azure', level: 3 },
      { name: 'Python Flask', level: 4 },
      { name: 'n8n Automation', level: 5 },
    ],
  },
  {
    id: 'security',
    label: 'Cloud & Security',
    icon: '🔐',
    color: 'warn',
    skills: [
      { name: 'Linux / RHEL', level: 5 },
      { name: 'Bash Scripting', level: 4 },
      { name: 'Network Security', level: 4 },
      { name: 'CISSP (Studying)', level: 3 },
      { name: 'CCSP (Studying)', level: 3 },
      { name: 'SIEM / SOC', level: 3 },
      { name: 'Penetration Testing', level: 3 },
      { name: 'ISO 27001 / NIST', level: 4 },
      { name: 'SCADA Security', level: 3 },
      { name: 'Zero Trust Frameworks', level: 3 },
      { name: 'Vulnerability Assessment', level: 4 },
      { name: 'PCI-DSS / CIS Controls', level: 3 },
    ],
  },
];
---

<section id="skills" class="skills-section">
  <div class="section-container">
    <div class="section-header reveal">
      <span class="section-label">Technical Arsenal</span>
      <h2 class="section-heading">
        Skills Across the <span class="gradient-text">Full AI Stack</span>
      </h2>
      <p class="section-desc">
        From raw data to deployed models — with the infrastructure and security knowledge to back it up.
      </p>
    </div>

    <!-- Domain tabs -->
    <div class="skills-tabs reveal">
      {skillDomains.map((domain, i) => (
        <button
          class={`skill-tab ${i === 0 ? 'active' : ''}`}
          data-domain={domain.id}
          aria-selected={i === 0}
        >
          <span class="tab-icon">{domain.icon}</span>
          <span class="tab-label">{domain.label}</span>
        </button>
      ))}
    </div>

    <!-- Skills panels -->
    {skillDomains.map((domain, i) => (
      <div
        class={`skills-panel ${i === 0 ? 'active' : ''}`}
        id={`panel-${domain.id}`}
        role="tabpanel"
      >
        <div class="skills-grid">
          {domain.skills.map(skill => (
            <div class={`skill-card color-${domain.color}`}>
              <div class="skill-info">
                <span class="skill-name">{skill.name}</span>
                <span class="skill-dots">
                  {[1,2,3,4,5].map(dot => (
                    <span class={`dot ${dot <= skill.level ? 'filled' : 'empty'}`}></span>
                  ))}
                </span>
              </div>
              <div class="skill-bar">
                <div
                  class="skill-bar-fill"
                  style={`width: ${skill.level * 20}%`}
                ></div>
              </div>
            </div>
          ))}
        </div>
      </div>
    ))}
  </div>
</section>

<style>
  .skills-section {
    padding: 100px 0;
    background: var(--bg-secondary);
    position: relative;
  }

  .section-header {
    text-align: center;
    margin-bottom: 48px;
  }

  .section-heading {
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 800;
    letter-spacing: -1px;
    margin: 12px 0 16px;
  }

  .section-desc {
    font-size: 16px;
    color: var(--text-muted);
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* Tabs */
  .skills-tabs {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 40px;
  }

  .skill-tab {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 20px;
    border-radius: 10px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--text-muted);
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .skill-tab:hover {
    color: var(--text-secondary);
    border-color: rgba(139, 92, 246, 0.3);
  }

  .skill-tab.active {
    background: rgba(139, 92, 246, 0.12);
    border-color: rgba(139, 92, 246, 0.5);
    color: var(--primary-light);
  }

  .tab-icon {
    font-size: 16px;
  }

  /* Panels */
  .skills-panel {
    display: none;
  }

  .skills-panel.active {
    display: block;
    animation: fade-up 0.4s ease-out;
  }

  @keyframes fade-up {
    from { opacity: 0; transform: translateY(16px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 12px;
  }

  .skill-card {
    padding: 16px 20px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    transition: all 0.2s ease;
  }

  .skill-card:hover {
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateY(-2px);
  }

  .skill-info {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .skill-name {
    font-size: 13px;
    font-weight: 500;
    color: var(--text-primary);
  }

  .skill-dots {
    display: flex;
    gap: 4px;
  }

  .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    transition: all 0.2s;
  }

  .color-purple .dot.filled { background: var(--primary-light); }
  .color-cyan .dot.filled { background: var(--accent); }
  .color-green .dot.filled { background: var(--success); }
  .color-warn .dot.filled { background: var(--warn); }

  .dot.empty {
    background: var(--border);
  }

  .skill-bar {
    height: 3px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }

  .skill-bar-fill {
    height: 100%;
    border-radius: 2px;
    transition: width 0.8s ease;
  }

  .color-purple .skill-bar-fill {
    background: linear-gradient(90deg, #7c3aed, #a78bfa);
  }
  .color-cyan .skill-bar-fill {
    background: linear-gradient(90deg, #0891b2, #22d3ee);
  }
  .color-green .skill-bar-fill {
    background: linear-gradient(90deg, #059669, #34d399);
  }
  .color-warn .skill-bar-fill {
    background: linear-gradient(90deg, #d97706, #fbbf24);
  }
</style>

<script>
  const tabs = document.querySelectorAll<HTMLButtonElement>('.skill-tab');
  const panels = document.querySelectorAll<HTMLElement>('.skills-panel');

  tabs.forEach(tab => {
    tab.addEventListener('click', () => {
      const domain = tab.dataset.domain;
      tabs.forEach(t => { t.classList.remove('active'); t.setAttribute('aria-selected', 'false'); });
      panels.forEach(p => p.classList.remove('active'));

      tab.classList.add('active');
      tab.setAttribute('aria-selected', 'true');
      document.getElementById(`panel-${domain}`)?.classList.add('active');
    });
  });
</script>
